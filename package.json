{
  "engines": {
    "node": ">=16.0.0"
  },
  "name": "booking-application",
  "private": true,
  "scripts": {
    "heroku-postbuild": "cd frontend/vite-project && npm install && npm run build"
  }
}
